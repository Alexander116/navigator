package api.settings;

/**
 * параметры конфигурационного файла
 */
public enum ConfigParam {
    PATH_TYPE,
    MAP_PATH,
    SPEED_MAP_PATH,
    BLOCK_POINT_PATH,
    OPTIMAL_COEFFICIENT,
    ROUTE_OUT_TYPE
}
